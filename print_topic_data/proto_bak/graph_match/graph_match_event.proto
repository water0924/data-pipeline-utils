syntax = "proto2";

package deeproute.graph_match;

message GraphMatchInfoEvent {
  // unit: ms
  optional uint32 during_time_ms = 1;
  // Route distance, unit: meters
  optional uint32 distance = 2;
}